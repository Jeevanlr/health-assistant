services:
  - type: web
    name: health-assistant
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r health-assistant/requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 60 --access-logfile - --error-logfile - wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
