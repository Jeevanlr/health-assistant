import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeeva.healthassistant',
  appName: 'health-assistant',
  webDir: 'build'
};

export default config;
